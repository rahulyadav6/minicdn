import axios from "axios";

// console.log("OriginClient BaseURL:", process.env.ORIGIN_URL);
// const originClient = axios.create({
//     baseURL: process.env.ORIGIN_URL,
// });

export default originClient;